# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| Executor_Router | implement a router to adapt different ai coding agent. | - .aicodingconfig 配置字段的具体 schema (有关 Agent 选择)配置字段按任务说明采用最小假设：{"AGENT_ROUTER_CONFIG":{"default_agent":"copilot","task_specific_agents":{}}}，并保留扩展位。<br>- 新代理明确使用OpenCode(见https://opencode.ai/docs/zh-cn/)，我希望使用它的CLI对它进行调用；<br>-  Copilot 继续使用现有 prompt reference，OpenCode 走独立命令执行链。<br>- task_specific_agents 的键应按技能名进行匹配，以当前扩展已有技能标识为准，并在实现时统一归一化。<br>- OpenCode CLI 的实际调用命令、参数格式、返回码约定、认证方式和 Windows 路径处理先实现命令构造与错误透传的适配层，待人工补充真实 CLI 契约。<br>- OpenCode 适配本轮先支持一次性执行与错误透传；更高级的会话控制标记为后续增强项。 | [2026-03-19] from LLM: 已完成 Executor_Router 路由改造，技术方案为在 `src/extension.ts` 中新增统一代理选择逻辑、OpenCode CLI 一次性执行适配层、`.aicodingconfig/.aicodingconfig.json` 配置读取兼容与 `AGENT_ROUTER_CONFIG` 保留写回，并保持 Copilot 仍沿用原有 prompt reference 提交链路。<br>- 变更文件: `src/extension.ts`、`.aicodingconfig`、`implementation/taskhelpinfos/2026-3-18_implement_a_router_to_adapt_different_ai_coding_agent.md`。<br>- 验证证据: `npm run compile` 已通过；`get_errors` 对上述变更文件返回 0 错误；本机 `Get-Command opencode` 结果为未安装，因此已确认 OpenCode 路径的命令构造与错误透传逻辑存在，但未完成真实 CLI 成功执行验收。<br>- 最终结果: 已满足默认代理读取、按技能路由、Copilot 保持原链路、OpenCode 错误透传和配置保留目标；剩余阻塞为需要人工安装并配置真实 OpenCode CLI 后再做端到端成功执行验收。 | ToDo | Verified | 1227 |
