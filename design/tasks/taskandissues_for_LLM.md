# Task And Issues For LLM

| Name | Problem | ProblemNotes | ResolverNotes | ProblemType | Status | Object_ID |
| --- | --- | --- | --- | --- | --- | --- |
| Application | 请帮我实现Architecture Reality Scanning & Reconciliation视图中的应用 | - reality.json 的精确 schema应至少被intention.json包含，这样才能进行匹配尝试，<br>- reality.json 的 schema至少对齐 elements、relationships、views 三类核心结构，无法稳定映射的语言细节先降级到扩展属性或 未知。<br>- ARCH_IMPL_GAP先输出机器可读 JSON 或 Markdown 报告各一份，放置在design/intention_reality_audit/下面,内容至少包含缺失元素、关系差异、原始指标、差异清单四类信息。<br>- Python 子进程先约定标准 CLI 协议 stdin/argv 输入、stdout JSON 输出、非 0 退出码失败，并保留后续兼容扩展位。脚本位置可暂定为独立扫描目录，后续再固化。<br>- TypeScript 扫描适配器优先采用 ts-morph；<br>- 当前任务先完成应用层能力和可调用入口，提供应用层内部服务入口,不需要显式 UI 触发点；<br>- 当前仓库既是“扫描器宿主”也是“被扫描目标系统”，但是可以设定哪些目录或文件需要排除在“被扫描目标系统”外；<br>- 支持显式传入排除列表，并将默认排除策略限制在扫描器内部，不扩散到协调服务。<br>- Seamless Fit Index / AAS 的计算先输出覆盖率/准确率/纯净度原始指标和差异清单，总分可选且不得硬编码阈值判定。<br>- TypeScript 与 Python 扫描器的实际代码路径在ast/目录下分别存放，如ast/ts,ast/py<br><br>**CONSTRAIN**<br>- JSON Format of Archimate Model（1194）是意图输入源，不应把运行时细节直接反写到 design/KG/SystemArchitecture.json<br><br><br> | <br>[2026-03-26] from LLM: 实现了基于现有 `ai4pb.runDesignCodeAlignment` 命令的应用层内部服务入口，新增 `src/architectureRealityService.ts` 负责扫描编排、统一 `reality.json`、协调差异计算与双格式报告输出；新增 `src/ast/ts/scanTypeScriptReality.ts` 使用 `ts-morph` 扫描 TS/JS；新增 `ast/py/scan_python_reality.py` 通过 stdin/argv 协议输出 Python 扫描 JSON；更新 `src/extension.ts` 以复用现有命令入口触发整条链路。验证证据：`npm run compile` 通过，且执行 `node -e "const svc = require('./out/architectureRealityService'); ..."` 成功生成 `design/intention_reality_audit/reality.json`、`design/intention_reality_audit/arch_impl_gap.json`、`design/intention_reality_audit/arch_impl_gap.md`。当前结果已满足入口、扫描、协调、报告落盘的主链路要求；剩余阻塞为当前匹配策略较保守，导致 coverage/accuracy 指标偏低，后续可继续增强意图/现实元素映射精度。 | ToDo | Active | 1156 |
